# One of these per trainee, written by ./provision.sh -- generated, not edited. Shown here
# so the shape is reviewable; the real files are gitignored because they carry credentials.
#
#   docker compose --env-file .env.platform --env-file .env.alice up -d

TRAINEE=alice
WORKSPACE_DIR=.workspaces/alice/01-legacy-fileshare
SCENARIO=01-legacy-fileshare

XILL4_PORT=8101
XILL4_DATABASE_CONNECTION_STRING=mongodb://alice:<generated>@mongo:27017/xill4_alice?authSource=xill4_alice

MONGO_DATABASE=xill4_alice
MONGO_USER=alice
MONGO_PASSWORD=<generated>

MINIO_PORT=9101
S3_ACCESS_KEY=trainee
S3_SECRET_KEY=<generated>
